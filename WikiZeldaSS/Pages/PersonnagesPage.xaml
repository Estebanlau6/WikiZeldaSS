<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:WikiZeldaSS.ViewModels"
             xmlns:model="clr-namespace:WikiZeldaSS.Models"
             x:Class="WikiZeldaSS.Pages.PersonnagesPage"
             Title="Wiki Zelda Skyward Sword"
             BackgroundColor="#1a1a2e"
             x:DataType="vm:PersonnagesViewModel">

    <ScrollView>
        <VerticalStackLayout Spacing="15" Padding="20">

            <Label Text="Personnages de Skyward Sword"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="#ffd700"
                   HorizontalOptions="Center"
                   Margin="0,10,0,20" />

            <CollectionView
                SelectionMode="Single"
                ItemsSource="{Binding Personnages}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:Personnage">
                        <Border BackgroundColor="#2c3e50" Padding="15" StrokeShape="RoundRectangle 10">
                            <VerticalStackLayout>
                                <Label Text="{Binding Emoji}" 
                                       FontSize="24"
                                       Margin="0,10,0,0" />
                                <Label Text="{Binding Nom}"
                                       FontSize="20"
                                       FontAttributes="Bold"
                                       TextColor="#3498db" />
                                <Label Text="{Binding Description}"
                                       FontSize="14"
                                       TextColor="White"
                                       LineBreakMode="WordWrap" />
                                <Label Text="{Binding Role}"
                                       FontSize="14"
                                       TextColor="#ecf0f1"
                                       Margin="0,5,0,0" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
